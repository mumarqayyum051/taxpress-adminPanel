// const baseURL = 'http://localhost:4000/api';
// const fileURL = 'http://localhost:4000/';
const baseURL = 'taxpress.api.quantux.net/api';
const fileURL = 'taxpress.api.quantux.net/';

const environment = {
  baseURL,
  fileURL,
};
export default environment;
